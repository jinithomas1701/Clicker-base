import React from 'react';
import {View} from 'react-native';

import ResetPasswordModel from '../../../components/molecules/profile/ResetPasswordModel';

const ResetPasswordTemplate = () => {
  return (
    <View viewStyle={{alignItems: 'center'}}>
      <ResetPasswordModel />
    </View>
  );
};

export default ResetPasswordTemplate;
